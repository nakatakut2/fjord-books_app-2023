<div class="field">
  <%= f.label :icon_image %><br />
  <p>現在設定中の画像</p>
    <% if resource.image.attached? %>
      <%= image_tag resource.image, width: 300 %>
    <% else %>
      <%= '：設定されていません' %>
    <% end %>
  <p><%= f.file_field :image %></p>
</div>

<div class="field">
  <%= f.label :name %><br />
  <%= f.text_field :name %>
</div>

<div class="field">
  <%= f.label :postal_code %><br />
  <%= f.text_field :postal_code %>
</div>

<div class="field">
  <%= f.label :address %><br />
  <%= f.text_field :address %>
</div>

<div class="field">
  <%= f.label :self_introduction %><br />
  <%= f.text_area :self_introduction %>
</div>
